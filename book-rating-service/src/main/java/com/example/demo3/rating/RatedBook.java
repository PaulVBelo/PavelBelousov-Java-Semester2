package com.example.demo3.rating;

public record RatedBook(String title,
                        String authorFirstName,
                        String authorLastName,
                        int rating
) {
}
